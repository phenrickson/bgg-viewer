/**
 * Anchor games: the handful of well-known titles drawn on top of the map with a label, so
 * the landscape reads as "Brass is here, Codenames is over there" rather than as a cloud.
 * Hand-curated — editorial, like the copy — and empty until Phil has explored the map and
 * chosen them. Labels are the game's catalog name; no editorial text lives here.
 */
export const ANCHORS: number[] = [];
